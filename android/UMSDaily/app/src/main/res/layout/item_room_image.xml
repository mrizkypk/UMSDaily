<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_room_image_layout"
    android:layout_margin="2dp"
    android:layout_width="75dp"
    android:layout_height="75dp">
    <ImageView
        android:id="@+id/item_room_image_thumbnail"
        android:scaleType="centerCrop"
        android:layout_width="75dp"
        android:layout_height="75dp" />
    <Button
        android:id="@+id/item_room_image_button_more"
        android:visibility="gone"
        android:background="@drawable/ic_keyboard_arrow_right"
        android:backgroundTint="#DDDDDD"
        android:layout_width="75dp"
        android:layout_height="75dp" />
    <ProgressBar
        android:id="@+id/item_room_image_progressbar"
        android:visibility="gone"
        android:indeterminateTint="@color/colorPrimary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />
</android.support.constraint.ConstraintLayout>